package utilities;

import org.openqa.selenium.WebDriver;

public class Waithelper {

	public WebDriver driver;
	
	public Waithelper(WebDriver driver) {
		this.driver=driver;
	}

	//we keep config readers, WebDriver managers, wait utilities, Excel readers for data-driven testing, screenshot handlers, and logging utilities
	
	
	
}
